engines:
  brakeman:
    enabled: true
  bundler-audit:
    enabled: true
  rubocop:
    enabled: true
  duplication:
    enabled: true
    exclude_paths:
      - "spec/"
    config:
      languages:
      - ruby
ratings:
  paths:
  - "app/**/*"
  - "**.rb"
exclude_paths:
- config/
- db/
- vendor/
